[alias]
lint = "clippy -- -D warnings -D clippy::all -D clippy::pedantic -D clippy::nursery -D clippy::perf" # -D clippy::cargo

[build]
rustflags = [
    # to allow us to use surrealkv as the underlying key-value store
    "--cfg",
    "surrealdb_unstable",
    # for profiling
    # "-C",
    # "symbol-mangling-version=v0",
]
